version: 2
build:
  os: ubuntu-latest

sphinx:
  configuration: docs/conf.py

python:
  version: "3.12"
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt

